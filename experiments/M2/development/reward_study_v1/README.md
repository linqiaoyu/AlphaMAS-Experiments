# M2 reward study v1

PRE-FORMAL DEVELOPMENT — NOT FORMAL M2.

The frozen market snapshot is used only to calculate matured TRAIN/VALIDATION reward labels. It is not agent evidence or a model input. Phase B must not fetch market data.
